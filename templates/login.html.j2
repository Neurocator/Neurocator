<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Neurocator!</title>
    <!-- Include OpenDyslexic Font -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=OpenDyslexic" type="text/css">
    <link rel="stylesheet" type="text/css" href="{{ url_for('static', filename='style.css') }}"/>
    <style>
        body, html {
            font-family: 'OpenDyslexic', sans-serif;
            margin: 0px;
            height: 100%;
            overflow: hidden;
        }

        .bg-img {
            background-image: url("{{ url_for('static', filename='Neurocator Prototype.jpg') }}"); /* Ensure the path is correct */
            height: 100%;
            width: 100%;
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            position: fixed;
            top: 0;
            left: 0;
            z-index: -1;
            display: block; /* Initially visible */
        }

        .content {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100%;
            text-align: center;
        }

        .welcome-message {
            font-size: xx-large;
            color: #333;
            margin-bottom: 20px;
        }

        .highlight {
            color: #333; /* Changed to a neutral color */
        }

        .form-container {
            background: rgba(255, 255, 255, 0.9);
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0px 0px 15px rgba(0,0,0,0.3);
            max-width: 600px;
            width: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .login-form {
            width: 100%;
        }

        .form-label {
            display: block;
            margin-bottom: 10px;
            font-weight: bold;
            color: #333;
            font-size: 1.2em;
        }

        .form-input {
            width: calc(100% - 20px);
            padding: 15px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 1.2em;
        }

        .remember-me-container {
            display: flex;
            align-items: center;
            margin-bottom: 20px;
        }

        .remember-me-checkbox {
            margin-right: 10px;
        }

        .remember-me-label {
            font-size: 1em;
            color: #333;
        }

        .login-button {
            background-color: #d8a7c5;
            color: white;
            border: none;
            padding: 15px 20px;
            font-size: 1.2em;
            cursor: pointer;
            border-radius: 5px;
            width: 100%;
            margin-bottom: 20px;
        }

        .login-button:hover {
            background-color: #c28ab4;
        }

        .signup-container {
            text-align: center;
            margin-top: 20px;
        }

        .signup-link {
            color: #d8a7c5;
            text-decoration: none;
            font-size: 1.1em;
        }

        .signup-link:hover {
            text-decoration: underline;
        }

        /* Slider switch CSS */
        .switch {
            position: relative;
            display: inline-block;
            width: 60px;
            height: 34px;
            margin-top: 20px;
        }

        .switch input {
            opacity: 0;
            width: 0;
            height: 0;
        }

        .slider {
            position: absolute;
            cursor: pointer;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: #ccc;
            transition: .4s;
            border-radius: 34px;
        }

        .slider:before {
            position: absolute;
            content: "";
            height: 26px;
            width: 26px;
            left: 4px;
            bottom: 4px;
            background-color: white;
            transition: .4s;
            border-radius: 50%;
        }

        input:checked + .slider {
            background-color: #2196F3;
        }

        input:checked + .slider:before {
            transform: translateX(26px);
        }

        .toggle-label {
            margin-top: 10px;
            font-size: 1em;
            color: #333;
        }
    </style>
</head>
<body>
    {% block body %}
    <div class="bg-img" id="backgroundImage"></div>
    <div class="content">
        <h1 class="welcome-message">Welcome to <span class="highlight">Neurocator</span><span>!</span></h1>
        <div class="form-container">
            <form method="POST" class="login-form">
                <label class="form-label"><b>Username</b></label>
                <input type="text" placeholder="Enter Username" name="username" class="form-input" required>
                <label class="form-label"><b>Password</b></label>
                <input type="password" placeholder="Enter Password" name="password" class="form-input" required>
                <div class="remember-me-container">
                    <input type="checkbox" name="remember" class="remember-me-checkbox">
                    <label class="remember-me-label">Remember me</label>
                </div>
                <button type="submit" class="login-button">Log In</button>
                <div class="signup-container">
                    <p>Don't have an account? <a href="signup" class="signup-link">Sign up!</a></p>
                </div>
            </form>
        </div>
        <div class="toggle-container">
            <label class="switch">
                <input type="checkbox" id="bgToggle">
                <span class="slider"></span>
            </label>
            <div class="toggle-label">Purple swirls on/off</div>
        </div>
    </div>
    <script>
        document.getElementById('bgToggle').addEventListener('change', function() {
    var bgImage = document.getElementById('backgroundImage');
    if (this.checked) {
        bgImage.style.display = 'none'; // Hide background when checked
        document.body.style.backgroundColor = '#FFF6EA'; // Set light cream color background
    } else {
        bgImage.style.display = 'block'; // Show background when unchecked
        document.body.style.backgroundColor = ''; // Remove background color
    }
});
    </script>
    {% endblock %}
</body>
</html>